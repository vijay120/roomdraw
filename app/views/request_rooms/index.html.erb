<h1>Listing request_rooms</h1>

<table>
  <tr>
    <th>User</th>
    <th>Room</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @request_rooms.each do |request_room| %>
  <tr>
    <td><%= request_room.user_id %></td>
    <td><%= request_room.room_id %></td>
    <td><%= link_to 'Show', request_room %></td>
    <td><%= link_to 'Edit', edit_request_room_path(request_room) %></td>
    <td><%= link_to 'Destroy', request_room, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Request room', new_request_room_path %>
