<%= form_for(@request_room) do |f| %>
  <% if @request_room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@request_room.errors.count, "error") %> prohibited this request_room from being saved:</h2>

      <ul>
      <% @request_room.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :room_id %><br />
    <%= f.number_field :room_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
