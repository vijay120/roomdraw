<div class="room_form">
<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <fieldset>
	<legend>Enter User Details</legend>

  <div>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <div >
    <%= f.label :email %><br />
    <%= f.text_field(:email) + " @hmc.edu" %>
  </div>

  <div>
	<%= f.label :cohort%><br />
    <%= f.select(:cohort, options_for_select([['Sophomore', 'Sophomore'], ['Junior', 'Junior'], ['Senior', 'Senior']])) %>
  </div>

  <div >
    <%= f.label :draw_number %><br />
    <%= f.text_field :draw_number %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
</fieldset>
<% end %>
</div>
